- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.config:
      create: true
      path: config 
    ~/.gitconfig: 

- create:
    - ~/Documents/Programming/

- shell:
  #- [git submodule update --init --recursive, Installing submodules]
  -
    command: ./test.zsh
    stdout: true
    stderr: true
