- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.config/nvim:
      create: true
      path: nvim
    ~/.gitconfig: 

- create:
    - ~/Documents/Programming/

- shell:
  #- [git submodule update --init --recursive, Installing submodules]
  -
    command: ./test.zsh
    stdout: true
    stderr: true
