local opts = {
  ensure_installed = {"vim", "lua", "typescript", "tsx", "python", "html", "javascript", "css", "cpp"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return opts
